{
  "name": "heyfam",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "pushd client && ng build && popd && pushd streamer && ng build && popd && node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "socket.io": "^3.0.4"
  },
  "nodemonConfig": {
    "ignore": ["client/dist/*"]
  }
}
